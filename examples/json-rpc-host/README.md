# JSON-RPC host example

Runs `examples/provider-hooks/workflow.cml` through `camlflow serve --stdio` and
answers effect requests from Node.

```sh
opam exec -- node examples/json-rpc-host/host.js
```
